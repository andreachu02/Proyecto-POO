#include <iostream>
#include "espacio.h"
#include"slots.h"


Calmacenamiento::almacenamiento(int largo, int ancho): largo_total{largo}, ancho_total{ancho}:
         {
    for (auto &posicion_x: slots)
        for (int i = 0; i < posicion_y; i++) {
            //row.push_back(casilla_t(hc, wc));
            posicion_x.emplace_back(hc, wc);
        }




